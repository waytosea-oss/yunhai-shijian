# 贡献指南

感谢你改进云海诗鉴。这个项目刻意保持小而清楚：一套数据契约、一个 Web 氛围屏、一个 Android `DreamService`，以及可选的 AI 生成器。

## 开始之前

1. 先搜索现有 Issue，确认问题没有被重复报告。
2. 功能讨论请说明使用终端、屏幕尺寸、数据来源和预期交互。
3. 涉及气象结论时，请区分图像观察、权威外部事实和模型推测。

## 本地检查

```bash
npm test
npm start
```

至少在一个桌面视口和一个横屏小视口中检查主屏、判读页和算力码表。修改 Android 代码时，再运行：

```bash
bash apps/android/build.sh
```

## 提交范围

- 保持改动聚焦，不顺手重构无关模块。
- 新增字段时，先更新 `docs/API.md`，并保持旧字段可读或写明破坏性变更。
- 不提交构建产物、调试签名、账号 Cookie、API Token、个人余额或本地绝对路径。
- 不提交授权不明确的卫星图片、地图底图、新闻全文、字体或印章素材。
- 演示数据必须明确标注为合成或虚构。

## Pull Request

PR 描述请包含：

- 改了什么以及为什么；
- 如何验证；
- 视觉改动的前后截图；
- 新数据或素材的来源与许可证；
- 仍然存在的限制。

提交贡献即表示你同意按 Apache License 2.0 授权所提交的代码。
