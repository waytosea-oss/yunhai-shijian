# 安全说明

## 报告问题

请通过 GitHub 仓库的 **Security > Report a vulnerability** 私下报告安全问题。不要在公开 Issue 中附上真实 API Key、Cookie、局域网拓扑或可复现的账户数据。

报告中请包含受影响版本、复现步骤、影响范围和建议缓解方式。维护者会先确认问题，再决定修复与披露时间。

## 部署注意

- 默认服务只监听 `127.0.0.1`。只有需要局域网终端访问时才设置 `HOST=0.0.0.0`。
- 内置服务没有身份认证，不应直接暴露到公网。
- `OPENAI_API_KEY` 只放在进程环境中，不要写入仓库、诗签 JSON 或前端代码。
- 算力码表只应接收聚合数值，不能把服务商 Cookie、密码或原始 Token 转发给浏览器。
- Android 示例允许明文 HTTP，目的是连接本地局域网服务；公网部署应改用 HTTPS 并收紧网络安全配置。

## 支持范围

安全更新优先覆盖 `main` 分支当前版本。演示数据不对应真实账号或实时天气。
